<h1>burgers!</h1>

<h2>burgers that are not devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="br">Burger Name: </label>
    <input type="text" id="br" name="burger_name" required>
  </div>

  <div class="form-group">
    <label for="ingredient">Toppings:</label><br>
    <input type="checkbox" name="ingredient_1" value="Bacon"> Bacon<br>
    <input type="checkbox" name="ingredient_2" value="Cheese"> Cheese<br>
    <input type="checkbox" name="ingredient_3" value="Lettuce"> Lettuce<br>
    <input type="checkbox" name="ingredient_4" value="Onion"> Onion<br>
    <input type="checkbox" name="ingredient_5" value="Pickles"> Pickles<br>
  </div>

  <div class="form-group">
    <label for="burger_picture">Burger Picture URL: </label><br>
    <input type="url" id="bp" name="burger_picture" required>
  </div>

  <button type="submit">Add burger</button>
</form>
